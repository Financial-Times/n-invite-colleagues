import { expect } from 'chai';
import sinon from 'sinon';
import fetchMock from 'fetch-mock';

import copyLink from '../main.js';

describe('DesktopAppBanner', () => {

	it('copies the contents of the link field to the clipboard', () => {
		expect.
	});

});
